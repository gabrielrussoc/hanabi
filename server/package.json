{
  "name": "hanabi",
  "version": "0.0.1",
  "description": "Homemade non-commercial implementation of Hanabi",
  "dependencies": {
    "express": "^4.17.1",
    "hanabi-types": "file:../hanabi-types"
  },
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node bin/start.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.9",
    "tslint": "^6.1.3",
    "typescript": "^4.1.2"
  }
}
